apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: payment-app-cert
  namespace: default
spec:
  dnsNames:
    - payment-app.pl
  secretName: payment-app-tls
  issuerRef:
    name: letsencrypt-issuer
    kind: ClusterIssuer